<!-- Generated by server/src/docs/generate.ts. Do not edit by hand: run `npm run docs` in server/ and commit the result. -->

# Profiling tools

2 tools. Not registered under `GODOT_MCP_PROFILE=core`; ask for it by name with `GODOT_MCP_CATEGORIES=profiling`.

[← All categories](README.md)

## `get_performance_monitors`

Return current values for all Godot Performance monitors grouped by category: time (FPS, frame/physics/navigation ms), memory (static bytes/peak), render (objects, primitives, draw calls, VRAM), physics_2d/3d (active objects, collision pairs, islands), audio (output latency), navigation (maps, regions, agents, polygons). Use 'category' to request a single group.

| Parameter | Type | Required | Description |
|---|---|:---:|---|
| `category` | `string` | — | Optional category filter. Omit to return all categories at once. One of: `"time"`, `"memory"`, `"render"`, `"physics_2d"`, `"physics_3d"`, `"audio"`, `"navigation"`. |

**Example** — the call the suite makes, from `e2e/blocks/22-profiling.json`, test PR-01:

```json
{}
```

## `get_memory_usage`

Return a breakdown of Godot's memory consumption: static allocator (used/peak in bytes and MB), render memory (VRAM, textures, buffers), and optionally a sample of currently cached resource paths. Useful for spotting memory leaks or unexpected resource retention.

| Parameter | Type | Required | Description |
|---|---|:---:|---|
| `include_resources` | `boolean` | — | If true (default), include a sample of paths from the resource cache showing which assets are currently loaded. |
| `max_resources` | `integer` | — | Maximum number of cached resource paths to return in the sample (default 50, max 500). |

**Example** — the call the suite makes, from `e2e/blocks/22-profiling.json`, test PR-03:

```json
{}
```
